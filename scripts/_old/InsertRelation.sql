--Insert the relation plus other info about it 
INSERT INTO tbl_Relations  
    (
        relationID,
        minX,
        minY,
        maxX,
        maxY,
        labelX,
        labelY
    )
    VALUES( ?, ?, ?, ?, ?, ?, ? );
